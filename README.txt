To run:
- Clone git repo locally
- Open index.html as a local file in a browser (note: Chrome doesn't support Web Notifications without https)

To build:
- npm install
- browserify main.js -o bundle.js
